ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  hosts:
    teleport.akoch.net:
    - /
  tls:
   - secretName: teleport-ingress-tls
     hosts:
     - teleport.akoch.net
