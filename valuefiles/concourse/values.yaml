postgresql:
  service:
    clusterIP:
web:
  service:
    api:
      clusterIP:
  ingress:
    enabled: true
    annotations:
      kubernetes.io/ingress.class: nginx
      kubernetes.io/tls-acme: "true"
      cert-manager.io/issuer: "letsencrypt-prod"
      nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    hosts:
      - concourse.akoch.net
    tls:
    - secretName: concourse-ingress-tls
      hosts:
      - concourse.akoch.net
